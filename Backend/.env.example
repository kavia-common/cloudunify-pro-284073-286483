# Server
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# CORS
# Single origin: https://app.example.com
# Multiple origins: https://app.example.com,https://admin.example.com
CORS_ORIGIN=*

# JWT configuration
# IMPORTANT: Set a strong secret in all environments
JWT_SECRET=replace-with-strong-secret
JWT_EXPIRES_IN=1h
JWT_ISSUER=cloudunify-pro
JWT_AUDIENCE=cloudunify-pro-frontend

# Database
# Prefer DATABASE_URL when available; otherwise set PG* vars below
# Example: postgresql://user:password@host:5432/dbname?sslmode=require
DATABASE_URL=
PGHOST=
PGPORT=5432
PGUSER=
PGPASSWORD=
PGDATABASE=
# Enable SSL by setting PGSSL=true for managed DBs that require TLS
PGSSL=false
PGPOOL_MAX=10

# Seeding and project definition discovery
# In production, set a strong random token and provide it via X-Seed-Token header
SEED_ADMIN_TOKEN=
# Optional override path to .projdefn directory containing seed JSON files
PROJDEFN_DIR=
