# CloudUnify Pro Backend environment example

# Server
PORT=3001
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info

# Database connection
# Preferred: single DATABASE_URL or individual PG* env vars.
# DATABASE_URL=postgresql://user:password@host:5432/cloudunify_pro
# Alternatively, set DB_CONNECTION_FILE to a file that contains a DSN (first token starting with postgresql:// or postgres://)
# DB_CONNECTION_FILE=/path/to/Database/db_connection.txt

# JWT configuration (required for auth routes)
# IMPORTANT: set a secure secret in non-dev environments.
JWT_SECRET=dev-secret-change-me
JWT_EXPIRES_IN=1h
JWT_ISSUER=cloudunify-pro
JWT_AUDIENCE=cloudunify-pro-frontend

# CORS
# Comma-separated list of allowed origins or "*" to allow all (default).
# Example for local dev Frontend at port 3000:
CORS_ORIGIN=http://localhost:3000,https://localhost:3000
