# CloudUnify Pro - Backend (.env.example)
# NOTE: The FastAPI app reads environment variables directly from the process.
#       Copy/adjust these into your deployment environment or export them in your shell.

# Server configuration
HOST=0.0.0.0
PORT=3001
LOG_LEVEL=info
NODE_ENV=development

# CORS configuration
# Comma-separated list of allowed origins or "*" to allow all (unsafe for production).
# If omitted, a sensible default is derived (e.g., http://localhost:3000).
CORS_ORIGIN=http://localhost:3000

# Preview / embedding (Kavia preview iframes)
# When true, allows embedding by adjusting security headers appropriately.
PREVIEW_MODE=false
# Optional override for CSP frame-ancestors when PREVIEW_MODE=true:
PREVIEW_FRAME_ANCESTORS='self' https://*.cloud.kavia.ai

# JWT / Auth configuration
# Required for authenticated endpoints; without it, protected dependencies will respond with 503.
JWT_SECRET=replace-with-a-strong-secret
# Optional: token expiry duration (supports 3600s, 15m, 2h, 7d). Default is 1h.
JWT_EXPIRES_IN=1h
# Optional issuer/audience/algorithm (defaults shown)
JWT_ISSUER=cloudunify-pro
JWT_AUDIENCE=cloudunify-pro-frontend
JWT_ALG=HS256

# Database configuration
# Preferred: a full async SQLAlchemy URL. If using postgres:// or postgresql://, the app will normalize it to async.
# Example: postgresql+asyncpg://user:password@host:5432/cloudunify_pro
DATABASE_URL=

# Optional: Path to a text file containing a PostgreSQL URL (fallback when DATABASE_URL is empty)
# Example: ../Database/db_connection.txt
DB_CONNECTION_FILE=
