# CloudUnify Pro Backend (.env.example)
# NOTE: The FastAPI app does NOT auto-load this file. These are sample values.
# Export these variables into your shell/process manager before running the server.
# Example (bash):
#   set -a; source .env; set +a

# Server
HOST=0.0.0.0
PORT=3001
LOG_LEVEL=info
NODE_ENV=development

# CORS / Frontend origin
# Comma-separated list or "*" to allow all. If unset, the app defaults to a sensible Frontend URL (typically http://localhost:3000).
CORS_ORIGIN=http://localhost:3000

# Preview embedding (Kavia)
# When true, adjusts headers to allow embedding inside preview iframes.
PREVIEW_MODE=true
# Content-Security-Policy frame-ancestors for preview mode.
PREVIEW_FRAME_ANCESTORS='self' https://*.cloud.kavia.ai

# Authentication (JWT)
# Required to enable protected endpoints and login.
JWT_SECRET=dev-insecure-secret-change-me
# Supported formats: "3600s", "15m", "2h", "7d"; default is 1h if unset.
JWT_EXPIRES_IN=1h
JWT_ISSUER=cloudunify-pro
JWT_AUDIENCE=cloudunify-pro-frontend
# JWT_ALG defaults to HS256 if not set
# JWT_ALG=HS256

# Database configuration
# Preferred: direct URL (auto-normalized to async driver for SQLAlchemy)
# Example: postgresql://<user>:<password>@<host>:<port>/<db>
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/cloudunify_pro
# Fallback: path to a text file containing the PostgreSQL URL (if DATABASE_URL is not set)
DB_CONNECTION_FILE=../Database/db_connection.txt

# Seeding (internal endpoints)
# Optional: path to a .projdefn directory for auto-discovery of mock data
PROJDEFN_DIR=../.projdefn
# Required header token for production seeding endpoints (X-Seed-Token)
SEED_ADMIN_TOKEN=dev-seed-token
