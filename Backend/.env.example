# Server
PORT=3001
HOST=0.0.0.0
LOG_LEVEL=info
CORS_ORIGIN=*

# JWT (required for auth-protected routes; health endpoints do not need this)
# JWT_SECRET=change-me
# JWT_EXPIRES_IN=1h
# JWT_ISSUER=cloudunify-pro
# JWT_AUDIENCE=cloudunify-pro-frontend

# Database (choose one: DATABASE_URL OR discrete PG* vars OR DB_CONNECTION_FILE)
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=postgres
# PGDATABASE=cloudunify_pro
# PGSSL=false
# DB_CONNECTION_FILE=/path/to/Database/db_connection.txt

# Internal seeding
# SEED_ADMIN_TOKEN=your-seed-admin-token
# PROJDEFN_DIR=/absolute/path/to/.projdefn
