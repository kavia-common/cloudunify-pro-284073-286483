# CloudUnify Pro Backend (.env.example)
# Note: The application does NOT auto-load this file. Export these variables into your process environment,
# or configure them in your runtime (container/orchestrator).

# Server
HOST=0.0.0.0
PORT=3001
LOG_LEVEL=info
NODE_ENV=development
REACT_APP_LOG_LEVEL=info

# CORS
# Comma-separated list of origins or "*" to allow all (dev). If not set, defaults to http://localhost:3000.
CORS_ORIGIN=http://localhost:3000

# Preview embedding (Kavia)
# Set to true in preview environments to allow iframe embedding.
PREVIEW_MODE=true
# Allowed frame ancestors in CSP when preview mode is enabled.
PREVIEW_FRAME_ANCESTORS='self' https://*.cloud.kavia.ai

# Authentication (JWT)
# JWT is only required for protected endpoints; the app can start without it.
# Use a strong secret in non-development environments.
JWT_SECRET=replace_this_with_a_secure_random_value_for_dev_only
JWT_EXPIRES_IN=1h
JWT_ISSUER=cloudunify-pro
JWT_AUDIENCE=cloudunify-pro-frontend

# Database (optional for startup)
# If DATABASE_URL is not set, the server will attempt to read DB_CONNECTION_FILE for a PostgreSQL URL.
# If neither is set, app startup will still succeed and health endpoints will be available.
# Example PostgreSQL URL format:
#   postgresql://user:password@host:5432/database
# The server will normalize to use asyncpg internally.
DATABASE_URL=
DB_CONNECTION_FILE=Database/db_connection.txt

# Seeding
# Optional path to .projdefn directory containing organizations/users/resources JSON for seeding.
PROJDEFN_DIR=
# In production, required to authorize seeding requests (sent as X-Seed-Token header).
SEED_ADMIN_TOKEN=changeme
