# Server
PORT=3001
HOST=0.0.0.0
LOG_LEVEL=info

# CORS (comma-separated origins or * for all)
CORS_ORIGIN=*

# JWT configuration
JWT_SECRET=replace-with-a-secure-random-string
JWT_EXPIRES_IN=1h
JWT_ISSUER=cloudunify-pro
JWT_AUDIENCE=cloudunify-pro-frontend

# Database (prefer DATABASE_URL)
# DATABASE_URL=postgresql://appuser:dbpass@localhost:5000/myapp

# Optional discrete PG variables if DATABASE_URL is not used
# PGHOST=localhost
# PGPORT=5000
# PGUSER=appuser
# PGPASSWORD=dbpass
# PGDATABASE=myapp
# PGSSL=false
# PGPOOL_MAX=10
# PGAPPNAME=cloudunify-pro-backend
# PGCONNECT_TIMEOUT_MS=5000
# PGIDLE_TIMEOUT_MS=30000

# Optional: explicit path to db_connection.txt.
# If DATABASE_URL is not set, the server will try to auto-detect.
# You can force a specific file here:
# DB_CONNECTION_FILE=../cloudunify-pro-284073-286484/Database/db_connection.txt

# Seeding
# If present, this overrides auto-discovery of .projdefn directory.
# PROJDEFN_DIR=/path/to/.projdefn
# Required in production to access internal seed endpoints:
# SEED_ADMIN_TOKEN=replace-with-a-strong-token
